FROM python:3.12-slim

WORKDIR /app
RUN apt-get update && apt-get install -y \
    build-essential \
    pkg-config \
    libsqlite3-dev \
 && rm -rf /var/lib/apt/lists/*

COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["python", "main.py", "run"]pandas
numpy
scikit-learn
python-dotenv
requests
websockets
joblib
kerykeion
ecdsa
eth-account>=0.11.0
websocket-clientpandas
numpy
scikit-learn
python-dotenv
requests
websockets
joblib
kerykeion
ecdsa
eth-account>=0.11.0
websocket-client
eth-account>=0.11.0
websocket-client